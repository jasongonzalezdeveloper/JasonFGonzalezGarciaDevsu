import { DateReleaseValidatorDirective } from './date-release-validator.directive';

xdescribe('DateReleaseValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new DateReleaseValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
