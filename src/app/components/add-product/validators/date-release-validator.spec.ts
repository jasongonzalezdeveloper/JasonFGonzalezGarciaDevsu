import { DateReleaseValidator } from './date-release-validator';

describe('DateReleaseValidator', () => {
  it('should create an instance', () => {
    const directive = new DateReleaseValidator();
    expect(directive).toBeTruthy();
  });
});
